<div class="py-5"><h1>Find the perfect job for you!</h1>
  <div class="container">
    <div class="row">
      <% @offers.each do |offer| %>
        <div class="offer">
          <h2><%= offer.company_id %></h2>
          <p><%= offer.description %></p>
          <p><%= offer.tag %></p>
        </div>
      <% end %>
    </div>
  </div>
    <p><%= link_to "Add a offert", new_offer_path %></p>
</div>
