 <div class="py-5"><h1>Find the perfect job for you!</h1>
  <div class="container">
    <div class="row">
      <% @offers.each do |offer| %>
      <div class="col-md-4">
        <div class="flat-card">
          <div class="flat-card-content">
            <div class="flat-card-title">
              <h4><%= offer %></h4>
              <p><%= offer %></p>
              <p><%= offer %></p>
            </div>
          </div>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>
